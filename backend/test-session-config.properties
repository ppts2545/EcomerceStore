# Test Session Configuration (Short timeout for testing)
server.servlet.session.timeout=2m
server.servlet.session.cookie.name=ECOMMERCE_SESSION_TEST
server.servlet.session.cookie.max-age=120
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
server.servlet.session.persistent=true
server.servlet.session.store-type=hash_map

# Use this config for testing session timeout
# Copy to application.properties to test 2-minute session timeout
