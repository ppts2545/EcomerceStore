# Copy this file to .env.local (for local dev) or set these in Vercel UI.
# Frontend variables MUST start with VITE_ to be exposed to the React app.

# ---------- TEST / DEVELOPMENT (Stripe test mode) ----------
VITE_API_BASE_URL=https://YOUR-BACKEND-TEST-URL
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxx
VITE_APP_ENV=test

# ---------- PRODUCTION (Stripe live mode) ----------
# Uncomment and fill when going live. Keep test values above for dev.
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxxxxxxxxxxxxxxxxxxxxxx
# VITE_APP_ENV=production

# (Optional) If you add feature flags use the VITE_ prefix:
# VITE_FEATURE_NEW_CHECKOUT=true

# DO NOT PUT ANY SECRET KEYS HERE.
# NEVER commit sk_test_ or sk_live_ keys.

# Backend (Spring Boot) expected environment / properties (set where backend runs, NOT here):
# stripe.secretKey=sk_test_xxx (or provided as environment variable STRIPE_SECRET_KEY)
# stripe.publishableKey=pk_test_xxx
# stripe.mode=test   # or live
# (Prod) stripe.secretKey=sk_live_xxx
# (Prod) stripe.publishableKey=pk_live_xxx
# (Prod) stripe.mode=live
