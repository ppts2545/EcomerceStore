{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Backend",
            "type": "shell",
            "command": "mvn spring-boot:run",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            }
        }
    ]
}
